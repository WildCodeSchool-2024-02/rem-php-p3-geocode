{% extends 'base.html.twig' %}

{% block body %}

    {% form_theme form 'bootstrap_5_layout.html.twig' %}
    <h1>Modification d'utilisateur</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Modifier</button>
    {{ form_end(form) }}

    <a href="{{ path('dashboard_userList') }}">back to list</a>
{% endblock %}
