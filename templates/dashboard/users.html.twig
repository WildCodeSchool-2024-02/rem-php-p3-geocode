{% extends 'base.html.twig' %}

{% block body %}
    <main class="dashboard-main">
        <ul class="user-list">
            {% for user in users %}
                <li>Utilisateur : {{ user.firstname }} {{ user.lastname }}, {{ user.email }}</li>
            {% endfor %}
        </ul>
    </main>
{% endblock %}
