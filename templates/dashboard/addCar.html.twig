{% extends 'base.html.twig' %}

{% block body %}

    {% form_theme form 'bootstrap_5_layout.html.twig' %}
    <h1>Ajout de véhicule électrique</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">Ajouter</button>
    {{ form_end(form) }}

    <a href="{{ path('dashboard_carList') }}">Retour à la liste</a>
{% endblock %}
